# Prompt Generator

The current iteration of this generator works on raw prompt snippets stored in a JSON file.
These snippets are combined into a final prompt based on user selection.

The user can then copy the generated prompt to the clipboard and use it on any genAI platform

## Next Steps
- Not hardcoding the selection creation
- Multiple prompt types for different models? - (maybe)
- Adding selection for model type, for which the user wants the prompt? - (maybe)
  
